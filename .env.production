# Production environment variables for Vercel deployment.
# NEXT_PUBLIC_ vars are bundled into client-side JS — not truly secret.
# Set NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID in the Vercel dashboard as well
# so it doesn't need to be committed if you rotate the key later.

NEXT_PUBLIC_AWS_URL="/"
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID="e9f2e73bba799a6a86e53af76325aace"
